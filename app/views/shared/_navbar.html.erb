<nav class="navbar navbar-default navbar-wagon" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">
        <%= image_tag "vector.png" %>
      </a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li>
          </li>
          <li class="dropdown">
            <%= link_to "#", {class: "dropdown-toggle", "data-toggle" => "dropdown", "role" => "button", "aria-expanded" => "false"} do %>
              <% if current_user.picture_file_name %>
                <%= image_tag current_user.picture.url(:medium), class: "img-avatar" %>
              <% end %>
              Profile <span class="caret"></span>
            <% end %>
            <ul class="dropdown-menu" role="menu">
              <li>
                <% if current_user.pepeato == true %>
                  <%= link_to profile_path do %>
                    <i class="fa fa-user"></i> <%= t ".Ma cuisine", default: "Ma cuisine" %>
                  <% end %>
                <% else %>
                  <%= link_to profile_path do %>
                    <i class="fa fa-user"></i> <%= t ".Mon profil", default: "Mon profil" %>
                  <% end %>
                <% end %>
              </li>
              <li>
                <% if current_user.pepeato == true %>
                    <%= link_to "#" do %>
                      <i class="fa fa-home"></i>  <%= t ".profile", default: "Mes recettes" %>
                    <% end %>
                  <% else %>
                    <%= link_to "#" do %>
                      <i class="fa fa-home"></i>  <%= t ".profile", default: "Mes commandes" %>
                    <% end %>

                <% end %>
              </li>
              <li>
                <%= link_to destroy_user_session_path, method: :delete do %>
                  <i class="fa fa-sign-out"></i>  <%= t ".sign_out", default: "Sign out" %>
                <% end %>
              </li>
            </ul>
          </li>
        <% else %>
          <li>
            <%= link_to t(".Devenir Pepeato", default: "Devenir Pepeato"), new_user_registration_path(pepeato: true) %>
          </li>
          <li>
            <%= link_to t(".Devenir Utilisateur", default: "Devenir Utilisateur"), new_user_registration_path %>
          </li>
          <li>
            <%= link_to t(".sign_in", default: "Sign in"), new_user_session_path %>
          </li>
        <% end %>
        <li>
          <%= link_to t(".top_call_to_action", default: "Trouve un plat"), "#", class: "btn" %>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
